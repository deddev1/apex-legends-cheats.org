---
import BaseLayout from '../layouts/BaseLayout.astro'
import { GameProductPage } from '../views/GameProductPage'
import { SEO, SITE_URL } from '../data/site'
import { productDetailJsonLd } from '../lib/seo'
import { getGame } from '../data/games'

const game = getGame('apex-legends')
const jsonLdExtra = [
  productDetailJsonLd(game?.status ?? 'Updating'),
  {
    '@type': 'BreadcrumbList',
    itemListElement: [
      { '@type': 'ListItem', position: 1, name: 'Home', item: `${SITE_URL}/` },
      {
        '@type': 'ListItem',
        position: 2,
        name: 'Apex Legends Cheats',
        item: `${SITE_URL}/apex-legends-cheats`,
      },
    ],
  },
]
---

<BaseLayout seo={SEO.product} jsonLdExtra={jsonLdExtra}>
  <GameProductPage client:idle guideSlug="apex-legends-cheats" />
</BaseLayout>


